#include "main.h"
/**
*_islower - Describes the main function
*
*Description:
*@c: is the parameter
* Return: int;
*/
int _isalpha(int c)
{
	if (c >= 'a' && c <= 'Z')
	{
		return (1);
	}

	else
	{
		return (0);
	}

}
